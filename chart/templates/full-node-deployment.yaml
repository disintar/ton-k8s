apiVersion: apps/v1
kind: Deployment
metadata:
  name: ton-mainnet-deployment
  labels:
    app: ton-mainnet
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ton-mainnet
  template:
    metadata:
      labels:
        app: ton-mainnet
    spec:
      containers:
      - name: ton-mainnet
        image: "{{ .Values.registry }}/ton/{{ .Values.images.tonFullNode.name }}:{{ .Values.images.tonFullNode.version }}"
        imagePullPolicy: {{ .Values.imagePullPolicy }}
        ports:
        - containerPort: {{ .Values.ports.tonFullNode.PUBLIC_PORT }}
          name: ton-pp
        - containerPort: {{ .Values.ports.tonFullNode.CONSOLE_PORT }}
          name: ton-cp
        - containerPort: {{ .Values.ports.tonFullNode.LITESERVER_PORT }}
          name: ton-lp
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ton-testnet-deployment
  labels:
    app: ton-testnet
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ton-testnet
  template:
    metadata:
      labels:
        app: ton-testnet
    spec:
      containers:
      - name: ton-testnet
        image: "{{ .Values.registry }}/ton/{{ .Values.images.testnetFullNode.name }}:{{ .Values.images.testnetFullNode.version }}"
        imagePullPolicy: {{ .Values.imagePullPolicy }}
        ports:
        - containerPort: {{ .Values.ports.testnetFullNode.PUBLIC_PORT }}
          name: testnet-pp
        - containerPort: {{ .Values.ports.testnetFullNode.CONSOLE_PORT }}
          name: testnet-cp
        - containerPort: {{ .Values.ports.testnetFullNode.LITESERVER_PORT }}
          name: testnet-lp
